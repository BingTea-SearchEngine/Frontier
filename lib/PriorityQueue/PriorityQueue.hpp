// PriorityQueue.hpp
#pragma once

#include <vector>

template<typename T>
class PriorityQueue {
public:
    PriorityQueue() {}

    void push(T elm) {}

    T pop() {return T();}

    std::vector<T> popN(size_t N) {return {};}

private:
};
